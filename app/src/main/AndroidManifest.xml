<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ryzko.nibu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Nibu"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application_name"
        android:logo="@drawable/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:node="replace">
        <activity
            android:name=".view.activities.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.DashboardActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".view.activities.MainActivity"
            android:label="@string/title_main" />
        <activity android:name=".view.activities.LoginActivity" />
        <activity android:name=".view.activities.RegisterActivity" />
        <activity android:name=".view.activities.BabiesListActivity" />
        <activity android:name=".view.activities.RoutineStreamActivity" />
        <activity android:name=".view.activities.ActivityBreastFeedDetails" />
        <activity android:name=".view.activities.ActivitySolidFeedDetails" />
    </application>

</manifest>